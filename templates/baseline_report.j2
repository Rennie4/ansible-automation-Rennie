<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Windows Server 2025 Baseline Audit</title>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 40px; line-height: 1.6; color: #333; background-color: #f9f9f9; }
        .container { background: white; padding: 30px; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .header { border-bottom: 3px solid #3498db; padding-bottom: 10px; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        th { background-color: #3498db; color: white; text-transform: uppercase; font-size: 0.85em; }
        .meta { color: #555; font-size: 0.9em; }
        .pass { color: green; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Windows Server 2025 Baseline Audit</h1>
            <div class="meta">
                <p><strong>Target Host:</strong> {{ inventory_hostname }}</p>
                <p><strong>Scan Performed:</strong> {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
                <p><strong>Operating System:</strong> {{ report_data.OsName }}</p>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th>SECURITY SETTING</th>
                    <th>CURRENT VALUE</th>
                    <th>COMPLIANCE STATUS</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Computer Name Verification</td>
                    <td>{{ report_data.CsName }}</td>
                    <td class="pass">COMPLIANT</td>
                </tr>
                <tr>
                    <td>OS Version Check</td>
                    <td>{{ report_data.OsVersion }}</td>
                    <td class="pass">COMPLIANT</td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>
