<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Windows Server 2025 Baseline Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; line-height: 1.6; color: #333; }
        .header { border-bottom: 2px solid #0056b3; padding-bottom: 10px; margin-bottom: 20px; }
        .status-pass { color: green; font-weight: bold; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        th { background-color: #f4f4f4; }
    </style>
</head>
<body>

    <div class="header">
        <h1>Security Baseline Report</h1>
        <p><strong>Target Host:</strong> {{ inventory_hostname }}</p>
        <p><strong>Scan Date:</strong> {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
    </div>

    <h3>System Information</h3>
    <table>
        <tr>
            <th>Attribute</th>
            <th>Value</th>
        </tr>
        <tr>
            <td>Computer Name</td>
            <td>{{ report_data.CsName }}</td>
        </tr>
        <tr>
            <td>Operating System</td>
            <td>{{ report_data.OsName }}</td>
        </tr>
        <tr>
            <td>OS Version</td>
            <td>{{ report_data.OsVersion }}</td>
        </tr>
    </table>

    <h3>Compliance Summary</h3>
    <p>The baseline scan for <strong>{{ report_data.CsName }}</strong> has been completed successfully and uploaded to S3.</p>

</body>
</html>
