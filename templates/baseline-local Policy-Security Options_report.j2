<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; background-color: #f0f2f5; margin: 0; padding: 30px; }
        .wrapper { max-width: 1100px; margin: auto; background: white; padding: 40px; border-radius: 12px; box-shadow: 0 5px 15px rgba(0,0,0,0.08); }
        h1 { color: #1a3a5a; border-bottom: 3px solid #0056b3; padding-bottom: 15px; margin-top: 0; }
        .metrics { display: flex; gap: 20px; margin: 25px 0; }
        .stat-card { flex: 1; padding: 20px; border-radius: 8px; border: 1px solid #dee2e6; border-left: 6px solid #0056b3; background: #f8f9fa; }
        .stat-card h3 { margin: 0; font-size: 0.75rem; color: #6c757d; text-transform: uppercase; letter-spacing: 1px; }
        .stat-card p { margin: 10px 0 0; font-size: 1.8rem; font-weight: bold; color: #1a3a5a; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background: #1a3a5a; color: white; text-align: left; padding: 14px; position: sticky; top: 0; }
        td { padding: 12px; border-bottom: 1px solid #eee; font-size: 0.95rem; }
        .Enabled { color: #28a745; font-weight: bold; }
        .Disabled { color: #dc3545; font-weight: bold; }
        .Not-Defined { color: #adb5bd; font-style: italic; }
    </style>
</head>
<body>
    <div class="wrapper">
        <h1>Windows Server 2025: Local Security Options Baseline</h1>
        <div class="metrics">
            <div class="stat-card"><h3>CIS Items Targeted</h3><p>107</p></div>
            <div class="stat-card" style="border-left-color: #28a745;"><h3>Total Found</h3><p>{{ scan_lines | length }}</p></div>
            <div class="stat-card" style="border-left-color: #17a2b8;"><h3>Target Host</h3><p>{{ inventory_hostname }}</p></div>
        </div>
        <table>
            <thead><tr><th>Policy Item</th><th>Current Value</th></tr></thead>
            <tbody>
                {% for line in scan_lines %}
                {% set parts = line.split('|') %}
                {% set val = parts[1] if parts|length > 1 else 'Not Defined' %}
                <tr>
                    <td>{{ parts[0] }}</td>
                    <td class="{{ val.split(' ')[0] if val != 'Not Defined' else 'Not-Defined' }}">{{ val }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
