<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Security Audit - {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 40px; background-color: #f4f7f6; }
        .box { background: white; padding: 25px; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h2 { color: #1a202c; border-bottom: 3px solid #3182ce; padding-bottom: 10px; }
        .summary { margin-bottom: 20px; color: #4a5568; font-size: 0.9em; }
        table { width: 100%; border-collapse: collapse; }
        th { background-color: #2d3748; color: white; padding: 12px; text-align: left; }
        td { padding: 10px; border-bottom: 1px solid #e2e8f0; font-size: 0.85em; }
        .Enabled { color: #38a169; font-weight: bold; }
        .Disabled { color: #e53e3e; font-weight: bold; }
        .Not.Defined { color: #a0aec0; font-style: italic; }
    </style>
</head>
<body>
    <div class="box">
        <h2>Windows Server 2025: Security Options Report</h2>
        <div class="summary">
            <strong>Hostname:</strong> {{ inventory_hostname }} | 
            <strong>Total Policies Mapped:</strong> {{ security_scan_raw.stdout_lines | length }}
        </div>
        <table>
            <thead>
                <tr>
                    <th>Policy Name</th>
                    <th>Security Setting</th>
                </tr>
            </thead>
            <tbody>
                {% for line in security_scan_raw.stdout_lines %}
                {% set parts = line.split('|') %}
                <tr>
                    <td>{{ parts[0] }}</td>
                    <td class="{{ parts[1] }}">{{ parts[1] }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
