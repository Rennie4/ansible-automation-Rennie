<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: sans-serif; margin: 20px; }
        table { width: 100%; border-collapse: collapse; }
        th { background-color: #333; color: white; padding: 10px; }
        td { padding: 8px; border: 1px solid #ccc; }
        .Enabled { color: green; font-weight: bold; }
        .Disabled { color: red; }
    </style>
</head>
<body>
    <h2>Security Report for {{ inventory_hostname }}</h2>
    <table>
        <tr>
            <th>Security Policy Name</th>
            <th>Configured Setting</th>
        </tr>
        {% for line in security_scan_raw.stdout_lines %}
        {% set p = line.split('|') %}
        <tr>
            <td>{{ p[0] }}</td>
            <td class="{{ p[1] }}">{{ p[1] }}</td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
