<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 30px; background-color: #f4f7f9; }
        .container { background: white; padding: 40px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); max-width: 1100px; margin: auto; }
        h1 { color: #1a2b3c; border-bottom: 4px solid #007bff; padding-bottom: 15px; margin-bottom: 30px; }
        .summary-box { display: flex; gap: 20px; margin-bottom: 40px; }
        .card { flex: 1; padding: 25px; border-radius: 10px; background: #fff; border: 1px solid #e2e8f0; border-top: 5px solid #007bff; text-align: center; }
        .card h3 { margin: 0; font-size: 0.85em; text-transform: uppercase; color: #64748b; }
        .card p { margin: 10px 0 0; font-size: 2.5em; font-weight: 800; color: #1e293b; }
        table { width: 100%; border-collapse: collapse; background: #fff; }
        th { background-color: #1e293b; color: white; padding: 15px; text-align: left; }
        td { padding: 12px 15px; border-bottom: 1px solid #f1f5f9; font-size: 0.95em; }
        .Enabled { color: #10b981; font-weight: bold; }
        .Disabled { color: #ef4444; font-weight: bold; }
        .Not-Defined { color: #94a3b8; font-style: italic; }
        tr:hover { background-color: #f8fafc; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Windows Server 2025: Security Options Audit</h1>
        <div class="summary-box">
            <div class="card">
                <h3>Total Targeted</h3>
                <p>104</p>
            </div>
            <div class="card" style="border-top-color: #10b981;">
                <h3>Successfully Pulled</h3>
                <p>{{ scan_output.stdout_lines | length }}</p>
            </div>
            <div class="card" style="border-top-color: #8b5cf6;">
                <h3>Host</h3>
                <p>{{ inventory_hostname }}</p>
            </div>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Security Policy Name</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                {% for line in scan_output.stdout_lines %}
                    {% set parts = line.split('|') %}
                    <tr>
                        <td>{{ parts[0] }}</td>
                        <td class="{{ parts[1] if parts[1] in ['Enabled', 'Disabled'] else 'Not-Defined' }}">
                            {{ parts[1] }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
