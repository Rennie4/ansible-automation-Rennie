<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CIS Audit Report - {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; background-color: #f0f4f8; margin: 0; padding: 25px; }
        .container { max-width: 1200px; margin: auto; background: white; padding: 30px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #1a3a5a; border-bottom: 3px solid #0056b3; padding-bottom: 10px; }
        .summary { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 30px; }
        .card { background: #eef6ff; padding: 20px; border-radius: 8px; border-left: 5px solid #0056b3; }
        .card h3 { margin: 0; color: #555; font-size: 0.8em; text-transform: uppercase; }
        .card p { margin: 10px 0 0; font-size: 1.8em; font-weight: bold; color: #1a3a5a; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background: #1a3a5a; color: white; text-align: left; padding: 12px; }
        td { padding: 10px; border-bottom: 1px solid #ddd; font-size: 0.9em; }
        .status-defined { color: #2d8a2d; font-weight: bold; }
        .status-missing { color: #b22222; font-style: italic; }
    </style>
</head>
<body>
    <div class="container">
        <h1>CIS Benchmark: Windows Server 2025 v1.0.0</h1>
        <div class="summary">
            <div class="card">
                <h3>Total Targeted</h3>
                <p>107</p>
            </div>
            <div class="card">
                <h3>Successfully Pulled</h3>
                <p>{{ scan_lines | length }}</p>
            </div>
            <div class="card">
                <h3>Target Host</h3>
                <p>{{ inventory_hostname }}</p>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th>Security Option Item</th>
                    <th>Current Status</th>
                </tr>
            </thead>
            <tbody>
                {% for line in scan_lines %}
                {% set parts = line.split('|') %}
                <tr>
                    <td>{{ parts[0] }}</td>
                    <td class="{{ 'status-missing' if parts[1] == 'Not Defined' else 'status-defined' }}">
                        {{ parts[1] }}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
