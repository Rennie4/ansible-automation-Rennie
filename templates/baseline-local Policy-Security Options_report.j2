<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Security Options Report - {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 20px; background-color: #f4f7f6; }
        h2 { color: #2c3e50; border-bottom: 2px solid #3498db; padding-bottom: 10px; }
        table { width: 100%; border-collapse: collapse; background-color: #fff; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        th, td { padding: 12px; text-align: left; border: 1px solid #ddd; }
        th { background-color: #34495e; color: white; text-transform: uppercase; letter-spacing: 1px; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        tr:hover { background-color: #f1f1f1; }
        .status-enabled { color: #27ae60; font-weight: bold; }
        .status-disabled { color: #c0392b; font-weight: bold; }
        .status-defined { color: #2980b9; }
        .footer { margin-top: 20px; font-size: 0.8em; color: #7f8c8d; }
    </style>
</head>
<body>

    <h2>Local Security Policies: Security Options</h2>
    <p><strong>Server Name:</strong> {{ inventory_hostname }}</p>
    <p><strong>Scan Date:</strong> {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>

    <table>
        <thead>
            <tr>
                <th>Policy</th>
                <th>Security Setting</th>
            </tr>
        </thead>
        <tbody>
            {% for line in security_scan_raw.stdout_lines %}
            {% set parts = line.split('|') %}
            <tr>
                <td>{{ parts[0] }}</td>
                <td class="{% if parts[1] == 'Enabled' %}status-enabled{% elif parts[1] == 'Disabled' %}status-disabled{% else %}status-defined{% endif %}">
                    {{ parts[1] }}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>Report generated by Ansible Automation Automation Sandbox</p>
        <p>Target S3 Bucket: oml-s3-automation-sandbox</p>
    </div>

</body>
</html>
