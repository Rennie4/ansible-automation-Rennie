<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', Arial; margin: 30px; background-color: #f4f7f9; }
        .container { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); max-width: 1200px; margin: auto; }
        h1 { color: #1a2b3c; border-bottom: 3px solid #007bff; padding-bottom: 12px; margin-bottom: 25px; }
        .summary-box { display: flex; gap: 20px; margin-bottom: 30px; }
        /* Matching your specific card style from image_f2bfb5.png */
        .card { flex: 1; padding: 25px; border-radius: 12px; background: #fff; border: 1px solid #e1e4e8; border-left: 8px solid #007bff; }
        .card h3 { margin: 0; font-size: 0.75em; text-transform: uppercase; color: #6a737d; font-weight: 800; }
        .card p { margin: 10px 0 0; font-size: 2.8em; font-weight: 700; color: #1a2b3c; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #1a2b3c; color: white; padding: 15px; text-align: left; }
        td { padding: 12px; border-bottom: 1px solid #eee; }
        .Enabled { color: #28a745; font-weight: 600; }
        .Disabled { color: #dc3545; font-weight: 600; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Windows Server 2025: Security Options (Local Policies)</h1>
        <div class="summary-box">
            <div class="card">
                <h3>SECURITY OPTIONS TARGET</h3>
                <p>{{ security_options_map.keys() | length }}</p>
            </div>
            <div class="card" style="border-left-color: #28a745;">
                <h3>SUCCESSFULLY AUDITED</h3>
                <p>{{ scan_output.stdout_lines | length }}</p>
            </div>
            <div class="card" style="border-left-color: #17a2b8;">
                <h3>HOST</h3>
                <p style="font-size: 2.2em;">{{ inventory_hostname }}</p>
            </div>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Policy Setting</th>
                    <th>Current State</th>
                </tr>
            </thead>
            <tbody>
                {% for line in scan_output.stdout_lines %}
                    {% set parts = line.split('|') %}
                    <tr>
                        <td>{{ parts[0] }}</td>
                        <td class="{{ parts[1] }}">{{ parts[1] }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
