<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 30px; background-color: #f4f7f6; }
        .container { background: white; padding: 40px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); max-width: 1150px; margin: auto; }
        h1 { color: #1a202c; border-bottom: 4px solid #3182ce; padding-bottom: 15px; margin-bottom: 30px; }
        .summary-box { display: flex; gap: 20px; margin-bottom: 40px; }
        .card { flex: 1; padding: 25px; border-radius: 10px; background: #fff; border: 1px solid #e2e8f0; border-top: 5px solid #3182ce; text-align: center; }
        .card h3 { margin: 0; font-size: 0.85em; text-transform: uppercase; color: #718096; }
        .card p { margin: 10px 0 0; font-size: 2.5em; font-weight: 800; color: #2d3748; }
        table { width: 100%; border-collapse: collapse; background: #fff; }
        th { background-color: #2d3748; color: white; padding: 15px; text-align: left; position: sticky; top: 0; }
        td { padding: 12px 15px; border-bottom: 1px solid #edf2f7; font-size: 0.95em; }
        .Enabled { color: #38a169; font-weight: bold; }
        .Disabled { color: #e53e3e; font-weight: bold; }
        .Not-Defined { color: #a0aec0; font-style: italic; }
        tr:hover { background-color: #f7fafc; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Windows Server 2025: Security Options Audit</h1>
        <div class="summary-box">
            <div class="card">
                <h3>Total Targeted</h3>
                <p>104</p>
            </div>
            <div class="card" style="border-top-color: #38a169;">
                <h3>Successfully Pulled</h3>
                <p>{{ scan_output.stdout_lines | length }}</p>
            </div>
            <div class="card" style="border-top-color: #805ad5;">
                <h3>Target Host</h3>
                <p>{{ inventory_hostname }}</p>
            </div>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Security Policy Name</th>
                    <th>Audit Result</th>
                </tr>
            </thead>
            <tbody>
                {% for line in scan_output.stdout_lines %}
                    {% set parts = line.split('|') %}
                    <tr>
                        <td>{{ parts[0] }}</td>
                        <td class="{{ parts[1] if parts[1] in ['Enabled', 'Disabled'] else 'Not-Defined' }}">
                            {{ parts[1] }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
