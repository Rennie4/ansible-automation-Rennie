<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Security Audit - {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 30px; background-color: #f3f4f6; }
        .box { background: white; padding: 25px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h2 { color: #111827; border-bottom: 3px solid #3b82f6; padding-bottom: 10px; }
        .meta { color: #6b7280; margin-bottom: 15px; font-size: 0.9em; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th { background-color: #1f2937; color: white; padding: 12px; text-align: left; font-size: 0.8em; }
        td { padding: 10px; border-bottom: 1px solid #e5e7eb; font-size: 0.85em; }
        tr:hover { background-color: #f9fafb; }
        .Enabled { color: #10b981; font-weight: bold; }
        .Disabled { color: #ef4444; font-weight: bold; }
        .Not.Defined { color: #9ca3af; font-style: italic; }
    </style>
</head>
<body>
    <div class="box">
        <h2>Windows Server 2025: Security Options Audit</h2>
        <div class="meta">
            <strong>Server:</strong> {{ inventory_hostname }} | 
            <strong>Policies Scanned:</strong> {{ security_scan_raw.stdout_lines | length }}
        </div>
        <table>
            <thead>
                <tr>
                    <th>Security Policy Name</th>
                    <th>Configured Setting</th>
                </tr>
            </thead>
            <tbody>
                {% for line in security_scan_raw.stdout_lines %}
                {% set parts = line.split('|') %}
                <tr>
                    <td>{{ parts[0] }}</td>
                    <td class="{{ parts[1] }}">{{ parts[1] }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
