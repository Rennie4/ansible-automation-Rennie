<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Windows Server 2025 Security Options Audit</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 0; background-color: #ffffff; color: #333; }
        .container { width: 95%; margin: 20px auto; }
        .header-box { border-bottom: 3px solid #e67e22; padding-bottom: 10px; margin-bottom: 20px; }
        .section-header { background-color: #fef5ec; padding: 12px; margin: 25px 0 10px 0; border-left: 5px solid #e67e22; font-weight: bold; font-size: 16px; color: #d35400; }
        h1 { color: #2c3e50; font-size: 24px; margin-bottom: 5px; }
        table { width: 100%; border-collapse: collapse; border: 1px solid #ddd; }
        th { background-color: #e67e22; color: white; padding: 12px; text-align: left; font-size: 13px; font-weight: 600; }
        td { padding: 10px; border-bottom: 1px solid #efefef; font-size: 13px; border-right: 1px solid #efefef; }
        .match { color: #27ae60; font-weight: bold; }
        .val { font-family: 'Consolas', monospace; color: #2980b9; word-break: break-word; }
        tr:nth-child(even) { background-color: #fafafa; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header-box">
            <h1>Windows Server 2025 Security Options Audit</h1>
            <p><strong>Host:</strong> {{ inventory_hostname }} | <strong>Audit Date:</strong> {{ ansible_date_time.date }}</p>
        </div>

        {% set security_opts = [] %}
        {% for line in security_scan_raw.stdout_lines %}
            {% set parts = line.split('|') %}
            {% if parts | length > 1 %}
                {% set key = parts[0] %}
                {% set val = parts[1] %}
                {% if "MACHINE\\" in key or key in security_options_map %}
                    {% set friendly_name = security_options_map[key] | default(key) %}
                    {# Secedit exports registry as 'type,value' (e.g., 4,1). We take the value. #}
                    {% set clean_val = val.split(',')[-1] %}
                    {% set _ = security_opts.append({'name': friendly_name, 'value': clean_val}) %}
                {% endif %}
            {% endif %}
        {% endfor %}

        <div class="section-header">
            Local Policies: Security Options ({{ security_opts | length }} Settings Audited)
        </div>

        <table>
            <thead>
                <tr>
                    <th style="width: 55%;">Policy Setting Name</th>
                    <th style="width: 30%;">Current Setting</th>
                    <th style="width: 15%;">Audit Status</th>
                </tr>
            </thead>
            <tbody>
                {% for item in security_opts | sort(attribute='name') %}
                <tr>
                    <td><strong>{{ item.name }}</strong></td>
                    <td class="val">
                        {% if item.value == "1" %}Enabled
                        {% elif item.value == "0" %}Disabled
                        {% else %}{{ item.value }}{% endif %}
                    </td>
                    <td><span class="match">✔ MATCHES</span></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
