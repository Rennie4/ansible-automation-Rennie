<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; background-color: #f8f9fa; margin: 0; padding: 25px; }
        .container { max-width: 1100px; margin: auto; background: #fff; padding: 35px; border-radius: 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        h1 { color: #212529; border-bottom: 3px solid #0d6efd; padding-bottom: 15px; }
        .summary-stats { display: flex; gap: 20px; margin-bottom: 30px; }
        .stat-item { flex: 1; padding: 20px; background: #e9ecef; border-radius: 8px; border-left: 5px solid #0d6efd; }
        .stat-item h4 { margin: 0; font-size: 12px; color: #6c757d; text-transform: uppercase; }
        .stat-item p { margin: 8px 0 0; font-size: 26px; font-weight: bold; color: #212529; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background: #343a40; color: #fff; text-align: left; padding: 12px; }
        td { padding: 10px; border-bottom: 1px solid #dee2e6; font-size: 14px; }
        .Enabled { color: #198754; font-weight: bold; }
        .Disabled { color: #dc3545; font-weight: bold; }
        .Not-Defined { color: #adb5bd; font-style: italic; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Windows Server 2025: Security Options Baseline Report</h1>
        <div class="summary-stats">
            <div class="stat-item"><h4>Targeted CIS Items</h4><p>107</p></div>
            <div class="stat-item"><h4>Items Audited</h4><p>{{ scan_lines | length }}</p></div>
            <div class="stat-item" style="border-left-color: #17a2b8;"><h4>Target Server</h4><p>{{ inventory_hostname }}</p></div>
        </div>
        <table>
            <thead><tr><th>Policy Item Description</th><th>Current Value</th></tr></thead>
            <tbody>
                {% for line in scan_lines %}
                {% set parts = line.split('|') %}
                {% set val = parts[1] if parts|length > 1 else 'Not Defined' %}
                <tr>
                    <td>{{ parts[0] }}</td>
                    <td class="{{ val.split(' ')[0] if val != 'Not Defined' else 'Not-Defined' }}">{{ val }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
