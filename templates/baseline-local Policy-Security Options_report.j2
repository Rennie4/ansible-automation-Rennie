<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', Arial; margin: 30px; background-color: #f4f7f9; }
        .container { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); max-width: 1200px; margin: auto; }
        h1 { color: #1a2b3c; border-bottom: 3px solid #007bff; padding-bottom: 12px; margin-bottom: 25px; }
        .summary-box { display: flex; gap: 20px; margin-bottom: 30px; }
        .card { flex: 1; padding: 20px; border-radius: 10px; background: #fff; border: 1px solid #e1e4e8; border-left: 6px solid #007bff; }
        .card h3 { margin: 0; font-size: 0.85em; text-transform: uppercase; color: #6a737d; letter-spacing: 1px; }
        .card p { margin: 10px 0 0; font-size: 2em; font-weight: 700; color: #1a2b3c; }
        table { width: 100%; border-collapse: collapse; }
        th { background-color: #1a2b3c; color: white; padding: 12px; text-align: left; position: sticky; top: 0; }
        td { padding: 10px 12px; border-bottom: 1px solid #eee; font-size: 0.9em; }
        .Enabled { color: #28a745; font-weight: 600; }
        .Disabled { color: #dc3545; font-weight: 600; }
        .Not-Defined { color: #999; font-style: italic; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Windows Server 2025: Security Options Audit</h1>
        <div class="summary-box">
            <div class="card">
                <h3>Total Targeted</h3>
                <p>104</p>
            </div>
            <div class="card" style="border-left-color: #28a745;">
                <h3>Successfully Pulled</h3>
                <p>{{ scan_output.stdout_lines | length }}</p>
            </div>
            <div class="card" style="border-left-color: #17a2b8;">
                <h3>Server Hostname</h3>
                <p>{{ inventory_hostname }}</p>
            </div>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Security Policy Name</th>
                    <th>Configured Setting</th>
                </tr>
            </thead>
            <tbody>
                {% for line in scan_output.stdout_lines %}
                    {% set parts = line.split('|') %}
                    {% set val = parts[1] if parts | length > 1 else 'Not Defined' %}
                    <tr>
                        <td>{{ parts[0] }}</td>
                        <td class="{{ val.split(' ')[0] if val != 'Not Defined' else 'Not-Defined' }}">
                            {{ val }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
