<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Segoe UI', sans-serif; margin: 30px; background-color: #f8f9fa; }
        .card { background: white; padding: 25px; border-radius: 8px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); max-width: 1100px; margin: auto; }
        h2 { color: #333; border-bottom: 2px solid #007bff; padding-bottom: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background: #343a40; color: white; padding: 12px; text-align: left; }
        td { padding: 10px; border-bottom: 1px solid #ddd; }
        .Enabled, .Administrator { color: #28a745; font-weight: bold; }
        .Disabled { color: #dc3545; font-weight: bold; }
        .Not.Defined { color: #888; font-style: italic; }
    </style>
</head>
<body>
    <div class="card">
        <h2>Windows Security Options Audit: {{ inventory_hostname }}</h2>
        <table>
            <thead>
                <tr><th>Policy Name</th><th>Setting</th></tr>
            </thead>
            <tbody>
                {% for line in security_scan_raw.stdout_lines %}
                {% set p = line.split('|') %}
                <tr>
                    <td>{{ p[0] }}</td>
                    <td class="{{ p[1] }}">{{ p[1] }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>
