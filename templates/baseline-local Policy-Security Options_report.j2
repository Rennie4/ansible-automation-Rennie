<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Windows Security Baseline Report - {{ inventory_hostname }}</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f7f7f7; padding: 20px; }
        h1, h2, h3 { color: #333; }
        .section { background: #ffffff; padding: 15px; margin-bottom: 20px; border-radius: 6px;
                   box-shadow: 0 0 6px rgba(0,0,0,0.1); }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        table, th, td { border: 1px solid #ccc; }
        th { background: #ececec; padding: 8px; }
        td { padding: 6px; }
        .muted { color: #777; font-size: 0.9em; }
    </style>
</head>
<body>

<h1>Windows Security Baseline Report</h1>
<p class="muted">
  Host: <strong>{{ inventory_hostname }}</strong><br/>
  Collected at: <strong>{{ json_report_data.CollectedAtUTC }}</strong>
</p>

{% set lsp = json_report_data.LocalSecurityPolicy %}

<div class="section">
  <h2>Account Policies (System Access)</h2>
  <table>
    <tr><th>Setting</th><th>Value</th></tr>
    {% for k,v in (lsp.SystemAccess or {}).items() %}
      <tr><td>{{ k }}</td><td>{{ v }}</td></tr>
    {% endfor %}
  </table>
</div>

<div class="section">
  <h2>Security Options (Registry Values)</h2>
  <table>
    <tr><th>Setting</th><th>Value</th></tr>
    {% for k,v in (lsp.RegistryValues or {}).items() %}
      <tr><td>{{ k }}</td><td>{{ v }}</td></tr>
    {% endfor %}
  </table>
</div>

<div class="section">
  <h2>User Rights Assignment (Privilege Rights)</h2>
  <table>
    <tr><th>Right</th><th>Assigned To</th></tr>
    {% for k,v in (lsp.PrivilegeRights or {}).items() %}
      <tr><td>{{ k }}</td><td>{{ v }}</td></tr>
    {% endfor %}
  </table>
</div>

<div class="section">
  <h2>Legacy Audit Policy</h2>
  <table>
    <tr><th>Setting</th><th>Value</th></tr>
    {% for k,v in (lsp.EventAuditLegacy or {}).items() %}
      <tr><td>{{ k }}</td><td>{{ v }}</td></tr>
    {% endfor %}
  </table>
</div>

<div class="section">
  <h2>Advanced Audit Policy</h2>
  <table>
    <tr><th>Category</th><th>Subcategory</th><th>Setting</th></tr>
    {% for row in (json_report_data.AdvancedAuditPolicy or []) %}
      <tr>
        <td>{{ row.Category }}</td>
        <td>{{ row.Subcategory }}</td>
        <td>{{ row.Setting }}</td>
      </tr>
    {% endfor %}
  </table>
</div>

<div class="section">
  <h2>Service Security Settings</h2>
  <table>
    <tr><th>Service</th><th>Value</th></tr>
    {% for k,v in (lsp.Services or {}).items() %}
      <tr><td>{{ k }}</td><td>{{ v }}</td></tr>
    {% endfor %}
  </table>
</div>

</body>
</html>
