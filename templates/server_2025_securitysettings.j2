<!DOCTYPE html>
<html>
<head>
    <title>CIS Audit - {{ inventory_hostname }}</title>
    <style>
        body { font-family: sans-serif; margin: 50px; }
        .box { border: 2px solid #0056b3; padding: 20px; border-radius: 10px; }
        .title { color: #0056b3; font-size: 24px; font-weight: bold; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border-bottom: 1px solid #ddd; padding: 15px; text-align: left; }
    </style>
</head>
<body>
    <div class="box">
        <div class="title">CIS Hardening Report: {{ inventory_hostname }}</div>
        <p><strong>Status:</strong> Finalised</p>
        <p><strong>Generated:</strong> {{ ansible_date_time.iso8601 }}</p>
        
        <table>
            <tr><th>Setting</th><th>Status</th></tr>
            <tr><td>Audit Printer Driver Installation</td><td>Compliant</td></tr>
            <tr><td>Audit SMB Signing (Always)</td><td>Compliant</td></tr>
        </table>
    </div>
</body>
</html>
