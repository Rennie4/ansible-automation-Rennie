<!DOCTYPE html>
<html>
<head>
    <title>CIS Hardening Report - {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 40px; color: #333; }
        .header { background-color: #003366; color: white; padding: 20px; border-radius: 5px; }
        .content { border: 1px solid #ddd; padding: 20px; margin-top: 20px; border-radius: 5px; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th, td { border: 1px solid #eee; padding: 12px; text-align: left; }
        th { background-color: #f4f4f4; }
    </style>
</head>
<body>
    <div class="header">
        <h1>CIS Security Audit: {{ inventory_hostname }}</h1>
        <p>Execution Date: {{ ansible_date_time.date }}</p>
    </div>
    <div class="content">
        <h3>Hardening Results</h3>
        <table>
            <tr><th>Control</th><th>Status</th></tr>
            <tr><td>2.3.2.1 Audit Printer Driver Installation</td><td>COMPLIANT</td></tr>
            <tr><td>2.3.9.1 Audit SMB Signing (Always)</td><td>COMPLIANT</td></tr>
        </table>
        <p style="font-size: 0.8em; color: #666; margin-top: 40px;">
            Target Bucket: oml-s3-automation-sandbox/cis-baseline-files
        </p>
    </div>
</body>
</html>
