<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 30px; background-color: #f4f4f9; }
        .report-box { background: white; padding: 25px; border-radius: 10px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); }
        h1 { color: #1a73e8; border-bottom: 2px solid #eee; padding-bottom: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #f8f9fa; color: #555; }
        .pass { color: #2e7d32; font-weight: bold; background: #e8f5e9; padding: 5px 10px; border-radius: 4px; }
        .fail { color: #c62828; font-weight: bold; background: #ffebee; padding: 5px 10px; border-radius: 4px; }
    </style>
</head>
<body>
    <div class="report-box">
        <h1>CIS Security Settings Audit: {{ ansible_hostname }}</h1>
        <p><strong>Platform:</strong> Windows Server 2025 | <strong>Execution Mode:</strong> CHECK</p>
        <table>
            <tr>
                <th>CIS ID</th>
                <th>Setting Name</th>
                <th>Audit Result</th>
            </tr>
            {% for item in audit_results %}
            <tr>
                <td>{{ item.id }}</td>
                <td>{{ item.name }}</td>
                <td>
                    {% if item.changed %}
                        <span class="fail">NON-COMPLIANT</span>
                    {% else %}
                        <span class="pass">COMPLIANT</span>
                    {% endif %}
                </td>
            </tr>
            {% endfor %}
        </table>
    </div>
</body>
</html>
