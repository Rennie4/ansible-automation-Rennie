<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CIS Windows Server 2025 Hardened Account Policies</title>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 40px; background-color: #f8f9fa; color: #333; }
        .container { background: white; padding: 35px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); max-width: 1100px; margin: auto; }
        .header { border-bottom: 4px solid #455A64; margin-bottom: 30px; padding-bottom: 10px; }
        h2 { color: #333; background: #E9ECEF; padding: 10px; border-radius: 4px; margin-top: 30px; font-size: 1em; text-transform: uppercase; font-weight: bold; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #455A64; color: white; padding: 12px; text-align: left; font-size: 0.85em; }
        td { padding: 12px; border-bottom: 1px solid #eee; font-family: 'Consolas', monospace; font-size: 0.95em; }
        .val-before { color: #c0392b; font-weight: bold; }
        .val-after { color: #27ae60; font-weight: bold; }
        .val-cis { color: #2980b9; font-style: italic; font-weight: bold; }
        .policy-col { width: 45%; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>CIS Benchmark Hardening Report</h1>
            <p><strong>Target Host:</strong> {{ inventory_hostname }} | <strong>OS:</strong> {{ before.OsName }}</p>
            <p><strong>Scan Date:</strong> {{ ansible_date_time.date }}</p>
        </div>

        <h2>1. Password Policy Comparison</h2>
        <table>
            <thead><tr><th class="policy-col">Policy Setting Name</th><th>Before</th><th>After (Hardened)</th><th>CIS Requirement</th></tr></thead>
            <tbody>
                <tr><td>Enforce password history</td><td class="val-before">{{ before.PasswordHistoryCount }}</td><td class="val-after">{{ after.PasswordHistoryCount }}</td><td class="val-cis">24 or more</td></tr>
                <tr><td>Maximum password age</td><td class="val-before">{{ before.MaximumPasswordAge }}</td><td class="val-after">{{ after.MaximumPasswordAge }}</td><td class="val-cis">1-365 days</td></tr>
                <tr><td>Minimum password age</td><td class="val-before">{{ before.MinimumPasswordAge }}</td><td class="val-after">{{ after.MinimumPasswordAge }}</td><td class="val-cis">1 or more</td></tr>
                <tr><td>Minimum password length</td><td class="val-before">{{ before.MinimumPasswordLength }}</td><td class="val-after">{{ after.MinimumPasswordLength }}</td><td class="val-cis">14 or more</td></tr>
                <tr><td>Relax minimum password length limits</td><td class="val-before">{{ before.RelaxPasswordLength }}</td><td class="val-after">{{ after.RelaxPasswordLength }}</td><td class="val-cis">Enabled</td></tr>
                <tr><td>Password must meet complexity</td><td class="val-before">{{ 'Enabled' if before.PasswordComplexity == '1' else 'Disabled' }}</td><td class="val-after">{{ 'Enabled' if after.PasswordComplexity == '1' else 'Disabled' }}</td><td class="val-cis">Enabled</td></tr>
                <tr><td>Store passwords using reversible encryption</td><td class="val-before">{{ 'Enabled' if before.ReversibleEncryption == '1' else 'Disabled' }}</td><td class="val-after">{{ 'Enabled' if after.ReversibleEncryption == '1' else 'Disabled' }}</td><td class="val-cis">Disabled</td></tr>
            </tbody>
        </table>

        <h2>2. Account Lockout Policy Comparison</h2>
        <table>
            <thead><tr><th class="policy-col">Policy Setting Name</th><th>Before</th><th>After (Hardened)</th><th>CIS Requirement</th></tr></thead>
            <tbody>
                <tr><td>Account lockout duration</td><td class="val-before">{{ before.LockoutDuration }}</td><td class="val-after">{{ after.LockoutDuration }}</td><td class="val-cis">15 or more minutes</td></tr>
                <tr><td>Account lockout threshold</td><td class="val-before">{{ before.LockoutThreshold }}</td><td class="val-after">{{ after.LockoutThreshold }}</td><td class="val-cis">1-5 attempts</td></tr>
                <tr><td>Reset account lockout counter</td><td class="val-before">{{ before.ResetLockoutCounter }}</td><td class="val-after">{{ after.ResetLockoutCounter }}</td><td class="val-cis">15 or more minutes</td></tr>
                <tr><td>Allow Administrator account lockout</td><td class="val-before">{{ before.AllowAdminLockout }}</td><td class="val-after">{{ after.AllowAdminLockout }}</td><td class="val-cis">Enabled</td></tr>
            </tbody>
        </table>
    </div>
</body>
</html>
