<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CIS Windows 2025 Comparison</title>
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 40px; background-color: #f8f9fa; color: #333; }
        .container { background: white; padding: 35px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); max-width: 1100px; margin: auto; }
        .header { border-bottom: 4px solid #455A64; margin-bottom: 30px; padding-bottom: 10px; }
        h2 { color: #333; background: #E9ECEF; padding: 10px; border-radius: 4px; margin-top: 30px; font-size: 1em; text-transform: uppercase; font-weight: bold; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #455A64; color: white; padding: 12px; text-align: left; font-size: 0.85em; }
        td { padding: 12px; border-bottom: 1px solid #eee; font-family: 'Consolas', monospace; font-size: 0.95em; }
        .val-before { color: #c0392b; font-weight: bold; }
        .val-after { color: #27ae60; font-weight: bold; }
        .val-cis { color: #2980b9; font-style: italic; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Standard Microsoft Defaults Baseline</h1>
            <p><strong>Target Host:</strong> {{ inventory_hostname }} | <strong>OS:</strong> {{ before.OsName }}</p>
            <p><strong>Scan Date:</strong> {{ ansible_date_time.date }}</p>
        </div>

        <h2>1. Password Policy Settings</h2>
        <table>
            <thead><tr><th>Policy Name</th><th>Before</th><th>After (Hardened)</th><th>CIS Requirement</th></tr></thead>
            <tbody>
                <tr><td>Enforce password history</td><td><span class="val-before">{{ before.PasswordHistoryCount }}</span></td><td><span class="val-after">{{ after.PasswordHistoryCount }}</span></td><td class="val-cis">24 or more</td></tr>
                <tr><td>Maximum password age</td><td><span class="val-before">{{ before.MaximumPasswordAge }}</span></td><td><span class="val-after">{{ after.MaximumPasswordAge }}</span></td><td class="val-cis">1-365 days</td></tr>
                <tr><td>Minimum password age</td><td><span class="val-before">{{ before.MinimumPasswordAge }}</span></td><td><span class="val-after">{{ after.MinimumPasswordAge }}</span></td><td class="val-cis">1 or more</td></tr>
                <tr><td>Minimum password length</td><td><span class="val-before">{{ before.MinimumPasswordLength }}</span></td><td><span class="val-after">{{ after.MinimumPasswordLength }}</span></td><td class="val-cis">14 or more</td></tr>
                <tr><td>Password must meet complexity</td><td><span class="val-before">{{ 'Enabled' if before.PasswordComplexity == '1' else 'Disabled' }}</span></td><td><span class="val-after">{{ 'Enabled' if after.PasswordComplexity == '1' else 'Disabled' }}</span></td><td class="val-cis">Enabled</td></tr>
            </tbody>
        </table>

        <h2>2. Account Lockout Policy Settings</h2>
        <table>
            <thead><tr><th>Policy Name</th><th>Before</th><th>After (Hardened)</th><th>CIS Requirement</th></tr></thead>
            <tbody>
                <tr><td>Account lockout duration</td><td><span class="val-before">{{ before.LockoutDuration }}</span></td><td><span class="val-after">{{ after.LockoutDuration }}</span></td><td class="val-cis">15+ minutes</td></tr>
                <tr><td>Account lockout threshold</td><td><span class="val-before">{{ before.LockoutThreshold }}</span></td><td><span class="val-after">{{ after.LockoutThreshold }}</span></td><td class="val-cis">1-5 attempts</td></tr>
                <tr><td>Allow Administrator account lockout</td><td><span class="val-before">{{ before.AllowAdminLockout }}</span></td><td><span class="val-after">{{ after.AllowAdminLockout }}</span></td><td class="val-cis">Enabled</td></tr>
            </tbody>
        </table>
    </div>
</body>
</html>
