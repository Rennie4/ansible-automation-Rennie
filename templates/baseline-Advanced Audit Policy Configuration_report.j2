<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Windows Server 2025 Advanced Audit Policy</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 40px; background-color: #f4f7f9; color: #333; }
        .container { background: white; padding: 35px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); max-width: 1100px; margin: auto; }
        .header { border-bottom: 4px solid #0078d4; margin-bottom: 30px; padding-bottom: 10px; }
        h1 { color: #2c3e50; font-size: 24px; margin: 0; }
        /* Grouped Header Style for Account Logon, etc. */
        h2 { background-color: #e1f0ff; color: #005a9e; padding: 10px 15px; border-left: 5px solid #0078d4; margin-top: 30px; font-size: 17px; text-transform: uppercase; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; background: #fff; }
        th { background-color: #0078d4; color: white; padding: 12px; text-align: left; font-size: 13px; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-size: 13px; }
        .val { font-family: 'Consolas', monospace; font-weight: bold; color: #005a9e; }
        /* Highlights Not Configured status in red */
        .not-configured { color: #d93025; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Advanced Audit Policy Configuration Audit</h1>
            <p><strong>Target Host:</strong> {{ inventory_hostname }} | <strong>Scan Date:</strong> {{ ansible_date_time.date }}</p>
        </div>

        {% if scan_results and scan_results | length > 0 %}
            {% for category, items in scan_results | groupby('Category') %}
            <h2>{{ category }}</h2>
            <table>
                <thead>
                    <tr>
                        <th style="width: 60%;">Subcategory</th>
                        <th>Audit Events</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                    <tr>
                        <td><strong>{{ item.Subcategory }}</strong></td>
                        <td class="val {% if item.Setting == 'Not Configured' %}not-configured{% endif %}">
                            {{ item.Setting }}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% endfor %}
        {% else %}
            <div style="background-color: #fee2e2; border: 1px solid #ef4444; padding: 20px; border-radius: 8px;">
                <p style="color: #b91c1c; font-weight: bold; margin: 0;">Error: No data retrieved from host.</p>
            </div>
        {% endif %}
    </div>
</body>
</html>
