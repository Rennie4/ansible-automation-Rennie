<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Advanced Audit Policy Audit</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 40px; background-color: #f4f7f9; color: #333; }
        .container { background: white; padding: 35px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); max-width: 1100px; margin: auto; }
        .header { border-bottom: 4px solid #0078d4; margin-bottom: 30px; padding-bottom: 10px; }
        /* MMC Group headers (e.g., Account Logon) */
        h2 { background-color: #e1f0ff; color: #005a9e; padding: 10px 15px; border-left: 5px solid #0078d4; margin-top: 30px; font-size: 17px; text-transform: uppercase; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th { background-color: #0078d4; color: white; padding: 12px; text-align: left; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-size: 13px; }
        .val { font-family: 'Consolas', monospace; font-weight: bold; color: #005a9e; }
        .not-configured { color: #d93025; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Advanced Audit Policy Configuration Baseline</h1>
            <p><strong>Host:</strong> {{ inventory_hostname }} | <strong>Date:</strong> {{ ansible_date_time.date }}</p>
        </div>

        {# Validation: Ensure we didn't just capture command help #}
        {% set first_item = scan_results[0].Subcategory if scan_results else '' %}
        {% if scan_results and first_item != '/?' %}
            {% for category, items in scan_results | groupby('Category') %}
            <h2>{{ category }}</h2>
            <table>
                <thead>
                    <tr>
                        <th style="width: 65%;">Subcategory</th>
                        <th>Audit Setting</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                    <tr>
                        <td><strong>{{ item.Subcategory }}</strong></td>
                        <td class="val {% if item.Setting == 'Not Configured' %}not-configured{% endif %}">
                            {{ item.Setting }}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% endfor %}
        {% else %}
            <div style="background: #fee2e2; border: 2px solid #ef4444; padding: 25px; border-radius: 8px; text-align: center;">
                <h3 style="color: #b91c1c; margin-top: 0;">Critical Data Error</h3>
                <p>The scan captured the <strong>Auditpol Help Menu</strong> instead of the actual system policy. Please verify the Ansible win_shell command switches.</p>
            </div>
        {% endif %}
    </div>
</body>
</html>
