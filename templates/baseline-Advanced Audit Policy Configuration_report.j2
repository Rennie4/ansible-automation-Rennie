<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Windows Server 2025 Advanced Audit Policy</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 40px; background-color: #f4f7f9; color: #333; }
        .container { background: white; padding: 35px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); max-width: 1000px; margin: auto; }
        .header { border-bottom: 4px solid #0078d4; margin-bottom: 30px; padding-bottom: 10px; }
        h1 { color: #2c3e50; font-size: 24px; margin: 0; }
        h2 { background-color: #e1f0ff; color: #005a9e; padding: 10px 15px; border-left: 5px solid #0078d4; margin-top: 30px; font-size: 18px; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th { background-color: #0078d4; color: white; padding: 12px; text-align: left; font-size: 14px; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-size: 13px; }
        .setting-val { font-weight: 600; color: #d32f2f; }
        .setting-configured { color: #2e7d32; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Advanced Audit Policy Baseline Report</h1>
            <p><strong>Server:</strong> {{ inventory_hostname }} | <strong>Status:</strong> Workgroup Auditing</p>
        </div>

        {% if scan_results is defined and scan_results | length > 0 %}
            {% for category, items in scan_results | groupby('Category') %}
            <h2>{{ category }}</h2>
            <table>
                <thead>
                    <tr>
                        <th style="width: 70%;">Subcategory</th>
                        <th>Current Configuration</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                    <tr>
                        <td>{{ item.Subcategory }}</td>
                        <td class="setting-val {% if item.Setting != 'No Auditing' %}setting-configured{% endif %}">
                            {{ item.Setting }}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </tbody>
            </table>
            {% endfor %}
        {% else %}
            <p style="color: red; font-weight: bold;">Error: Data parsing failed. Please check the Ansible 'Parse Audit Data into List' task output.</p>
        {% endif %}
    </div>
</body>
</html>
