<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Windows 2025 Advanced Audit Policy</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 40px; background-color: #f4f7f9; }
        .container { background: white; padding: 35px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); max-width: 1000px; margin: auto; }
        .header { border-bottom: 4px solid #0078d4; margin-bottom: 30px; padding-bottom: 10px; }
        /* Style for headers like Account Logon, Detailed Tracking, etc. */
        h2 { background-color: #e1f0ff; color: #005a9e; padding: 10px 15px; border-left: 5px solid #0078d4; margin-top: 30px; font-size: 17px; text-transform: uppercase; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th { background-color: #0078d4; color: white; padding: 12px; text-align: left; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-size: 14px; }
        .val { font-family: 'Consolas', monospace; font-weight: bold; color: #005a9e; }
        .not-configured { color: #d93025; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Advanced Audit Policy Configuration Baseline</h1>
            <p><strong>Host:</strong> {{ inventory_hostname }} | <strong>Date:</strong> {{ ansible_date_time.date }}</p>
        </div>

        {% if scan_results and scan_results | length > 0 %}
            {% for category, items in scan_results | groupby('Category') %}
            <h2>{{ category }}</h2>
            <table>
                <thead>
                    <tr>
                        <th style="width: 65%;">Subcategory</th>
                        <th>Audit Events</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                    <tr>
                        <td><strong>{{ item.Subcategory }}</strong></td>
                        <td class="val {% if item.Setting == 'Not Configured' %}not-configured{% endif %}">
                            {{ item.Setting }}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% endfor %}
        {% else %}
            <p style="color: #d93025; font-weight: bold; border: 2px solid #d93025; padding: 15px; border-radius: 8px;">
                Error: No audit data captured. Ensure the target host supports Advanced Audit Configuration.
            </p>
        {% endif %}
    </div>
</body>
</html>
