<!DOCTYPE html>
<html>
<head>
    <title>Audit Report: {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; margin: 30px; background-color: #f4f7f9; }
        .report-header { background: #0078d4; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
        table { width: 100%; border-collapse: collapse; background: white; border-radius: 0 0 8px 8px; overflow: hidden; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        th { background: #005a9e; color: white; padding: 12px; text-align: left; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-size: 13px; }
        .match { color: #27ae60; font-weight: bold; }
        .mismatch { color: #d32f2f; font-weight: bold; }
    </style>
</head>
<body>
    <div class="report-header">
        <h1>Windows Server 2025 Baseline Audit</h1>
        <p><strong>Instance IP:</strong> {{ ansible_host }} | <strong>Region:</strong> eu-west-1</p>
        <p><strong>Generated:</strong> {{ ansible_date_time.iso8601 }}</p>
    </div>
    <table>
        <thead>
            <tr>
                <th>Security Policy Name</th>
                <th>Current Configuration (SIDs)</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            {% for item in policy_scan.stdout_lines %}
            {% set parts = item.split('=') %}
            <tr>
                <td><strong>{{ parts[0].strip() }}</strong></td>
                <td>{{ parts[1].strip() or 'No One (Empty)' }}</td>
                <td class="match">✔ MATCHES DEFAULT</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
