<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Windows Server 2025 – Local Policy Audit</title>
  <style>
    body { font-family: Arial, Helvetica, sans-serif; background-color: #f5f6fa; margin: 20px; }
    h1, h2 { color: #2c3e50; }
    table { width: 100%; border-collapse: collapse; background-color: #ffffff; font-size: 13px; }
    th, td { border: 1px solid #dcdcdc; padding: 8px; text-align: left; }
    th { background-color: #2c3e50; color: #ffffff; }
    .ok { background-color: #e8f6ef; }
    .fail { background-color: #fdecea; }
  </style>
</head>

<body>

<h1>Windows Server 2025</h1>
<h2>Local User Rights – Factory Baseline Audit</h2>

<table>
  <thead>
    <tr>
      <th>Policy</th>
      <th>Description</th>
      <th>Expected</th>
      <th>Evaluated</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
  {% for r in audit_results.results %}
    <tr class="{{ 'fail' if r.changed else 'ok' }}">
      <td>{{ r.name }}</td>
      <td>{{ r.display }}</td>
      <td>{{ r.value }}</td>
      <td>{{ r.evaluated if r.evaluated else 'Not Defined' }}</td>
      <td>{{ 'NON-COMPLIANT' if r.changed else 'COMPLIANT' }}</td>
    </tr>
  {% endfor %}
  </tbody>
</table>

</body>
</html>
``
