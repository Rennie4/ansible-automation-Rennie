<!DOCTYPE html>
<html>
<head>
    <title>Audit Report - {{ inventory_hostname }}</title>
    <style>
        body { font-family: Segoe UI, Tahoma, sans-serif; margin: 30px; }
        .header { background-color: #004a99; color: white; padding: 15px; border-radius: 5px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ccc; padding: 12px; text-align: left; }
        th { background-color: #f2f2f2; }
        .pass { color: #28a745; font-weight: bold; }
        .fail { color: #dc3545; font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Windows Server 2025 Local Policy Audit</h1>
        <p>Host: {{ inventory_hostname }} | Date: {{ ansible_date_time.iso8601 }}</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>Policy Setting (User Right)</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            {% for item in audit_results.results %}
            <tr>
                <td>{{ item.item }}</td>
                <td>
                    {% if item.changed == false %}
                    <span class="pass">Compliant</span>
                    {% else %}
                    <span class="fail">Non-Compliant</span>
                    {% endif %}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
