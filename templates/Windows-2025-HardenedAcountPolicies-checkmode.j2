<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: sans-serif; margin: 30px; background: #f4f7f6; }
        .box { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        h2 { background: #E9ECEF; padding: 10px; font-size: 14px; text-transform: uppercase; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th { background: #455A64; color: white; text-align: left; padding: 10px; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-family: monospace; }
        .diff { color: #d9534f; font-weight: bold; }
    </style>
</head>
<body>
    <div class="box">
        <h1>Full CIS Audit: {{ inventory_hostname }}</h1>
        
        <h2>1. Password Policy Comparison</h2>
        <table>
            <tr><th>Policy</th><th>Current</th><th>CIS Requirement</th></tr>
            <tr><td>Enforce password history</td><td class="{{ 'diff' if data.PasswordHistory|int < 24 }}">{{ data.PasswordHistory }}</td><td>24 or more</td></tr>
            <tr><td>Max password age</td><td>{{ data.MaxPasswordAge }}</td><td>1-365 days</td></tr>
            <tr><td>Min password age</td><td>{{ data.MinPasswordAge }}</td><td>1 or more</td></tr>
            <tr><td>Min password length</td><td class="{{ 'diff' if data.MinPasswordLength|int < 14 }}">{{ data.MinPasswordLength }}</td><td>14 or more</td></tr>
            <tr><td>Relax min password length</td><td>{{ data.RelaxLength }}</td><td>Enabled (1)</td></tr>
            <tr><td>Password complexity</td><td>{{ data.PasswordComplexity }}</td><td>Enabled</td></tr>
        </table>

        <h2>2. Account Lockout Policy Comparison</h2>
        <table>
            <tr><th>Policy</th><th>Current</th><th>CIS Requirement</th></tr>
            <tr><td>Lockout duration</td><td>{{ data.LockoutDuration }}</td><td>15 or more</td></tr>
            <tr><td>Lockout threshold</td><td class="diff">{{ data.LockoutThreshold }}</td><td>1-5 attempts</td></tr>
            <tr><td>Observation window</td><td>{{ data.LockoutWindow }}</td><td>15 or more</td></tr>
            <tr><td>Allow Admin Lockout</td><td>{{ data.AllowAdminLockout }}</td><td>Enabled (1)</td></tr>
        </table>
    </div>
</body>
</html>
