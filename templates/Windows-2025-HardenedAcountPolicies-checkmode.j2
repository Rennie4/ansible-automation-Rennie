<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CIS Check Mode Report - {{ inventory_hostname }}</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 40px; background-color: #f8f9fa; }
        .container { background: white; padding: 30px; border-radius: 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); max-width: 900px; margin: auto; }
        .banner { background-color: #fff3cd; color: #856404; padding: 15px; border: 1px solid #ffeeba; border-radius: 4px; text-align: center; font-weight: bold; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #455A64; color: white; padding: 12px; text-align: left; }
        td { padding: 12px; border-bottom: 1px solid #eee; font-family: 'Consolas', monospace; }
        .out-of-compliance { color: #d9534f; font-weight: bold; }
        .in-compliance { color: #5cb85c; }
    </style>
</head>
<body>
    <div class="container">
        <div class="banner">PREVIEW MODE: This report shows current compliance gaps. No changes were made.</div>
        <h1>Hardening Audit: {{ inventory_hostname }}</h1>
        <p><strong>Operating System:</strong> {{ before.OsName }}</p>

        <table>
            <thead>
                <tr>
                    <th>Policy Setting</th>
                    <th>Current State</th>
                    <th>CIS Required State</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Relax minimum password length limits</td>
                    <td>{{ before.RelaxPasswordLength }}</td>
                    <td>Enabled (1)</td>
                    <td class="{{ 'out-of-compliance' if before.RelaxPasswordLength != 'Enabled (1)' else 'in-compliance' }}">
                        {{ 'FIX REQUIRED' if before.RelaxPasswordLength != 'Enabled (1)' else 'COMPLIANT' }}
                    </td>
                </tr>
                <tr>
                    <td>Account Lockout Threshold</td>
                    <td>{{ before.LockoutThreshold }}</td>
                    <td>5</td>
                    <td class="{{ 'out-of-compliance' if before.LockoutThreshold != '5' else 'in-compliance' }}">
                        {{ 'FIX REQUIRED' if before.LockoutThreshold != '5' else 'COMPLIANT' }}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>
