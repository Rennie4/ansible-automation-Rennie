<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', sans-serif; margin: 30px; }
        .header { background: #2c3e50; color: white; padding: 20px; border-radius: 5px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        .compliant { color: green; font-weight: bold; }
        .non-compliant { color: red; font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h1>CIS Audit: Windows Server 2025</h1>
        <p>Host: {{ ansible_hostname }} | Mode: CHECK</p>
    </div>
    <table>
        <tr>
            <th>CIS ID</th>
            <th>Setting Name</th>
            <th>Status</th>
        </tr>
        {% for item in audit_results %}
        <tr>
            <td>{{ item.id }}</td>
            <td>{{ item.name }}</td>
            <td class="{{ 'non-compliant' if item.changed else 'compliant' }}">
                {{ 'NON-COMPLIANT' if item.changed else 'COMPLIANT' }}
            </td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
